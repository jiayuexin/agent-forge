# 7. 技术选型与项目架构

> **文档层级**: 第二层 · 设计规格
> **文档类型**: 设计规格
> **文档状态**: 已定稿
> **文档版本**: docs-v0.2
> **最后更新**: 2026-06-18
> **实现状态**: 未开始
>
> 框架的依赖选型、Monorepo 目录结构。

## 命名规范

- 工具名: kebab-case (query-order, create-refund)
- Agent 包名: agent-<role> (agent-customer-service)
- CLI 命令: agentforge <command>
- API 路径: /api/ 前缀

## 7.1 核心依赖

| 包名 | 版本 | 用途 | 必选 |
|---|---|---|---|
| `typescript` | ^5.4 | 类型系统 | ✅ |
| `openai` | ^4.x | OpenAI API 客户端 | ✅ |
| `@anthropic-ai/sdk` | ^0.30 | Anthropic API 客户端 | 可选 |
| `ollama` | ^0.5 | Ollama 本地模型客户端 | 可选 |
| `express` | ^4.21 | HTTP 服务 | ✅ |
| `ws` | ^8.18 | WebSocket | ✅ |
| `ejs` | ^3.1 | 模板引擎（代码生成） | ✅ |
| `commander` | ^12.1 | CLI 命令框架 | ✅ |
| `chalk` | ^5.3 | 终端彩色输出 | ✅ |
| `eventemitter3` | ^5.0 | 事件系统 | ✅ |
| `zod` | ^3.23 | 运行时数据校验 | ✅ |
| `uuid` | ^10.0 | ID 生成 | ✅ |

## 7.2 开发依赖

| 包名 | 版本 | 用途 |
|---|---|---|
| `vitest` | ^2.0 | 测试框架 |
| `tsx` | ^4.19 | TypeScript 执行 |
| `eslint` | ^9.x | 代码规范 |
| `prettier` | ^3.x | 代码格式化 |
| `typedoc` | ^0.26 | API 文档生成 |

## 7.3 生成的 Agent 的依赖

```json
{
  "dependencies": {
    "@agentforge/core": "^1.0.0"
  },
  "peerDependencies": {
    "openai": "^4.x"
  }
}
```

## 7.4 框架 Monorepo 结构

```
agentforge/
├── packages/
│   ├── core/                      # 核心运行时
│   │   ├── src/
│   │   │   ├── agent/
│   │   │   │   ├── IAgent.ts      # Agent 接口定义
│   │   │   │   ├── BaseAgent.ts   # Agent 抽象基类
│   │   │   │   └── AgentLifeCycle.ts
│   │   │   ├── runtime/
│   │   │   │   ├── AgentRegistry.ts   # Agent 注册中心
│   │   │   │   ├── AgentExecutor.ts   # 任务执行器
│   │   │   │   └── MiddlewareChain.ts  # 中间件链
│   │   │   ├── provider/
│   │   │   │   ├── IProvider.ts       # Provider 接口
│   │   │   │   ├── OpenAIProvider.ts
│   │   │   │   ├── AnthropicProvider.ts
│   │   │   │   └── OllamaProvider.ts
│   │   │   ├── plugin/
│   │   │   │   ├── IPlugin.ts
│   │   │   │   └── PluginManager.ts
│   │   │   ├── generator/
│   │   │   │   ├── AgentGenerator.ts   # 生成器主入口
│   │   │   │   ├── PromptBuilder.ts    # Prompt 构建器
│   │   │   │   ├── TemplateEngine.ts   # 模板引擎
│   │   │   │   ├── SkillMatcher.ts     # 技能匹配器
│   │   │   │   └── CodeEmitter.ts      # 代码发射器
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   ├── types/                     # 共享类型定义
│   │   ├── src/
│   │   │   ├── agent.ts
│   │   │   ├── config.ts
│   │   │   ├── task.ts
│   │   │   ├── result.ts
│   │   │   ├── plugin.ts
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   ├── sdk/                       # SDK 集成包
│   │   ├── src/
│   │   │   ├── AgentFramework.ts      # 框架主类
│   │   │   ├── Pipeline.ts            # 编排流水线
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   ├── cli/                       # CLI 工具
│   │   ├── src/
│   │   │   ├── commands/
│   │   │   │   ├── create.ts
│   │   │   │   ├── batch.ts
│   │   │   │   ├── serve.ts
│   │   │   │   ├── list.ts
│   │   │   │   ├── run.ts
│   │   │   │   └── dashboard.ts
│   │   │   └── index.ts
│   │   └── package.json
│   │
│   └── dashboard/                 # Web 管理面板
│       ├── src/
│       │   ├── pages/
│       │   │   ├── Home.tsx
│       │   │   ├── AgentList.tsx
│       │   │   ├── AgentCreate.tsx
│       │   │   ├── Playground.tsx
│       │   │   └── Monitor.tsx
│       │   ├── components/
│       │   ├── api/
│       │   ├── store/
│       │   └── App.tsx
│       ├── index.html
│       ├── vite.config.ts
│       └── package.json
│
├── templates/                     # Agent 代码模板
│   ├── base/                      # 通用基础模板
│   │   ├── index.ts.ejs
│   │   ├── prompts.ts.ejs
│   │   ├── tools.ts.ejs
│   │   ├── types.ts.ejs
│   │   ├── config.ts.ejs
│   │   ├── test.ts.ejs
│   │   ├── package.json.ejs
│   │   ├── tsconfig.json.ejs
│   │   └── README.md.ejs
│   │
│   └── roles/                     # 岗位专属模板
│       ├── customer-service/
│       ├── sales-assistant/
│       ├── code-reviewer/
│       ├── content-writer/
│       └── data-analyst/
│
├── examples/                      # 使用示例
│   ├── quick-start/
│   ├── batch-generation/
│   ├── http-integration/
│   ├── sdk-pipeline/
│   └── python-integration/
│
├── package.json
├── pnpm-workspace.yaml
├── tsconfig.base.json
└── README.md
```

## 7.5 生成的 Agent 项目结构

```
agents/agent-customer-service/
├── src/
│   ├── index.ts                  # 入口，导出 Agent 类
│   ├── agent.ts                  # Agent 核心实现
│   ├── prompts.ts                # 系统提示词定义
│   ├── tools.ts                  # 工具定义和实现
│   └── types.ts                  # 类型定义
├── tests/
│   └── agent.test.ts             # 单元测试
├── dist/                         # 编译产物
├── package.json
├── tsconfig.json
├── .agentforge.json              # Agent 元数据（框架识别用）
└── README.md
```

## 7.6 架构设计原则

| 原则 | 说明 |
|---|---|
| 接口优先 | 所有 Agent 遵循 `IAgent` 统一接口，保证可替换性 |
| 插件化 | 核心功能通过 Middleware 和 Plugin 机制扩展 |
| 零依赖生成 | 生成的 Agent 核心依赖 ≤ 2 个（含 devDependencies 的设计目标为 ≤ 5 个），避免依赖地狱 |
| 约定优于配置 | 合理默认值，80% 场景零配置即可使用 |
| 可观测性 | 内置日志、指标、追踪，方便监控和调试 |
