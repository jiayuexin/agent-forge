# 附录 A：生成 ClientAgent 示例

> **文档层级**: 第二层 · 设计规格
> **文档类型**: 设计规格
> **文档状态**: 已定稿
> **文档版本**: docs-v0.4
> **最后更新**: 2026-06-23
> **实现状态**: 未开始
>
> 本附录展示从岗位描述生成的 ClientAgent 客户端应用的完整文件结构。类型定义与生成流程详见 [03-生成引擎.md](./03-生成引擎.md)，接口规范详见 [01-核心设计.md](./01-核心设计.md)。

## 输入描述

> "一个处理用户咨询和投诉的客服Agent，能查询订单状态、处理退款请求、发送通知"

## 生成结果概览

生成的 ClientAgent 位于 `./client-agents/customer-service/`，文件结构如下：

```
./client-agents/customer-service/
├── src/
│   ├── main.ts              # 守护进程入口
│   ├── agent.ts             # ClientAgent 类实现
│   ├── prompts.ts           # System Prompt
│   ├── tools.ts             # 工具定义
│   ├── types.ts             # 类型定义
│   └── runtime.ts           # AgentRuntimeClient 配置
├── package.json
├── tsconfig.json
├── README.md
└── .agentforge/
    ├── config.json          # Agent 元数据 + Hub 端点
    └── security.json        # 本地命令执行授权
```

**`src/prompts.ts`（自动生成）：**

```typescript
export const SYSTEM_PROMPT = `
# 客服专员

## 身份定义
你是一个专业的客服专员，负责处理用户的咨询、投诉和售后问题。
你的目标是提供高效、友好、专业的客户服务体验。

## 核心能力
1. 查询订单状态和物流信息
2. 处理退款和退货申请
3. 解答常见产品和服务问题
4. 主动发送订单状态更新通知
5. 识别并上报高风险投诉

## 行为规范
- 始终使用友好、专业的语气
- 在解决问题前先表达理解和同理心
- 对无法立即解决的问题，给出明确的时间预期
- 涉及金额操作时，必须确认用户身份
- 不得承诺超出权限范围的赔偿方案

## 工具使用
你可以使用以下工具来辅助工作：
- query-order: 查询订单详情和物流信息
- process-refund: 发起退款申请
- send-notification: 发送短信/邮件通知

## 输出格式
回复用户时：
1. 使用简洁明了的语言
2. 涉及订单时提供关键信息（单号、状态、时间）
3. 如有后续操作步骤，使用编号列表说明

结构化输出时：
- intent: 用户意图分类（query/refund/complaint/inquiry）
- orderId: 关联订单号
- action: 建议操作
- priority: 处理优先级（low/medium/high/urgent）

## 限制条件
- 不得泄露其他用户的订单信息
- 退款金额超过500元需要主管审批
- 工作时间外的投诉记录但不立即处理
`;
```

**`src/tools.ts`（自动生成）：**

```typescript
import { ToolDefinition } from '@agentforge/core';

export const customerServiceTools: ToolDefinition[] = [
  {
    name: 'query-order',
    description: '查询订单的详细信息和当前状态',
    parameters: {
      type: 'object',
      properties: {
        orderId: {
          type: 'string',
          description: '订单号，如 ORD-001',
        },
      },
      required: ['orderId'],
    },
    handler: async (params: { orderId: string }) => {
      // 实际项目中替换为真实的数据查询逻辑
      return {
        status: 'shipped',
        carrier: '顺丰速运',
        trackingNumber: 'SF1234567890',
        estimatedDelivery: '2026-03-28',
      };
    },
  },
  {
    name: 'process-refund',
    description: '处理用户的退款申请',
    parameters: {
      type: 'object',
      properties: {
        orderId: { type: 'string', description: '订单号' },
        reason: { type: 'string', description: '退款原因' },
        amount: { type: 'number', description: '退款金额' },
      },
      required: ['orderId', 'reason'],
    },
    handler: async (params) => {
      return { refundId: 'REF-001', status: 'processing', eta: '3-5个工作日' };
    },
  },
  {
    name: 'send-notification',
    description: '向用户发送通知（短信或邮件）',
    parameters: {
      type: 'object',
      properties: {
        userId: { type: 'string' },
        type: { type: 'string', enum: ['sms', 'email'] },
        content: { type: 'string' },
      },
      required: ['userId', 'type', 'content'],
    },
    handler: async (params) => {
      return { sent: true, messageId: 'MSG-001' };
    },
  },
];
```

**`src/agent.ts`（自动生成）：**

```typescript
import { BaseAgent, AgentConfig } from '@agentforge/core';
import { IClientAgent } from '@agentforge/types';
import { SYSTEM_PROMPT } from './prompts.js';
import { customerServiceTools } from './tools.js';

export class CustomerServiceAgent extends BaseAgent implements IClientAgent {
  constructor() {
    super({
      name: 'customer-service',
      role: '客服专员',
      version: '1.0.0',
      capabilities: [
        { name: 'order_query', description: '查询订单状态和物流' },
        { name: 'refund', description: '处理退款申请' },
        { name: 'notification', description: '发送用户通知' },
      ],
    });
  }

  protected getSystemPrompt(): string {
    return SYSTEM_PROMPT;
  }

  protected getDefaultTools() {
    return customerServiceTools;
  }

  async startDaemon(): Promise<void> { /* ... */ }
  async stopDaemon(): Promise<void> { /* ... */ }
  async connectToHub(hubUrl: string, token: string): Promise<void> { /* ... */ }
  async disconnectFromHub(): Promise<void> { /* ... */ }
  getLocalCapabilityCache(): Capability[] { return []; }
  getLocalCommandAuthorization(): LocalCommandAuthLevel { return 'disabled'; }
}
```

**`src/runtime.ts`（自动生成）：**

```typescript
import { AgentRuntimeClient } from '@agentforge/runtime-client';
import { CustomerServiceAgent } from './agent.js';

export function createRuntime() {
  const agent = new CustomerServiceAgent();
  return new AgentRuntimeClient(agent, {
    hubUrl: process.env.AGENTFORGE_HUB_URL,
    authToken: process.env.AGENTFORGE_NODE_TOKEN,
  });
}
```

**`src/main.ts`（自动生成）：**

```typescript
import { createRuntime } from './runtime.js';

const runtime = createRuntime();

await runtime.start();

process.on('SIGINT', async () => {
  await runtime.stop();
  process.exit(0);
});
```

**`package.json`（自动生成）：**

```json
{
  "name": "agent-customer-service",
  "version": "1.0.0",
  "description": "处理用户咨询和投诉的客服 ClientAgent",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc",
    "run": "node dist/main.js",
    "test": "vitest"
  },
  "dependencies": {
    "@agentforge/core": "^1.0.0",
    "@agentforge/runtime-client": "^1.0.0"
  },
  "peerDependencies": {
    "openai": "^4.x"
  }
}
```

**`.agentforge/config.json`（自动生成）：**

```json
{
  "id": "customer-service",
  "name": "customer-service",
  "role": "客服专员",
  "version": "1.0.0",
  "templateId": "customer-service",
  "hubUrl": "wss://hub.example.com",
  "capabilityCacheDir": "./.agentforge/capabilities"
}
```

**`.agentforge/security.json`（自动生成）：**

```json
{
  "localCommandAuth": {
    "level": "disabled",
    "whitelist": [],
    "requireConfirmationFor": []
  },
  "allowRemoteExecution": true,
  "requireLocalConfirmation": ["refund", "send-notification"]
}
```
