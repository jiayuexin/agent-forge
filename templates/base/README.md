# Base Template

This is the base template used by `AgentGenerator` to scaffold a new ClientAgent.

It contains the shared project skeleton (entry point, runtime wiring, configuration files) that every generated agent extends.
