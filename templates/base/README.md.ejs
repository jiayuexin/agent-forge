# <%= agentRole %> Agent

<%= packageId %> — auto-generated by @agentforge/core

## Overview

This agent acts as a **<%= agentRole %>**.

### Capabilities

<% capabilities.forEach(function(cap) { %>- <%= cap %>
<% }); %>

## Installation

\`\`\`bash
npm install <%= packageId %>
\`\`\`

## Usage

\`\`\`typescript
import { createAgent } from '<%= packageId %>';

const agent = createAgent();
\`\`\`

## Configuration

See `src/config.ts` for default configuration. You can override any setting:

\`\`\`typescript
const agent = createAgent({
  model: { modelName: 'gpt-4o', temperature: 0.5 },
});
\`\`\`

## Tools

<% tools.forEach(function(tool) { %>- **<%= tool.name %>**: <%= tool.description %>
<% }); %>

## License

MIT
