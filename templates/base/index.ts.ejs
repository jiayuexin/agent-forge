import { <%= agentName.split('-').map(s => s.charAt(0).toUpperCase() + s.slice(1)).join('') %>Agent } from './agent';
import { SYSTEM_PROMPT } from './prompts';
import { TOOLS } from './tools';
import { AgentConfig } from './types';
import { DEFAULT_CONFIG } from './config';

export { <%= agentName.split('-').map(s => s.charAt(0).toUpperCase() + s.slice(1)).join('') %>Agent } from './agent';
export { SYSTEM_PROMPT } from './prompts';
export { TOOLS } from './tools';
export { DEFAULT_CONFIG } from './config';
export type { AgentConfig } from './types';

/**
 * Create a pre-configured <%= agentRole %> agent.
 */
export function create<%= agentName.split('-').map(s => s.charAt(0).toUpperCase() + s.slice(1)).join('') %>Agent(
  config?: Partial<AgentConfig>,
) {
  return new <%= agentName.split('-').map(s => s.charAt(0).toUpperCase() + s.slice(1)).join('') %>Agent({
    ...DEFAULT_CONFIG,
    ...config,
  });
}

export default create<%= agentName.split('-').map(s => s.charAt(0).toUpperCase() + s.slice(1)).join('') %>Agent;
